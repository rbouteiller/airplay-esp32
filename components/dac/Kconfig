menu "DAC Configuration"

    choice DAC_TYPE
        prompt "DAC Type"
        default DAC_TAS57XX if BOARD_SQUEEZEAMP_COMMON
        default DAC_NONE
        help
            Select the DAC driver to use. The DAC handles digital-to-analog
            conversion and amplifier control for audio output.

        config DAC_NONE
            bool "None"
            help
                No hardware DAC. Volume control is done in software.

        config DAC_TAS57XX
            bool "TI TAS57xx (TAS5754/TAS5756)"
            help
                TI TAS5754M / TAS5756M DAC/amplifier as used on SqueezeAMP.
    endchoice

    config DAC_CONTROLS_VOLUME
        bool
        default y if !DAC_NONE
        help
            Hidden option. Set automatically when a hardware DAC is selected.
            When true, the DAC handles volume control and software attenuation
            is skipped in the audio output path.

endmenu
